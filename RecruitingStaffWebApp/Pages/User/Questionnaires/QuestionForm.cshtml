@page
@model RecruitingStaff.WebApp.Pages.User.Questionnaires.QuestionFormModel
@{
}
<div class="row justify-content-center">
    <div class="col-7">
        <form method="post">
            <input type="hidden" asp-for="@Model.Question.Id" />
            <input type="hidden" name="questionnaireId" value="@Model.QuestionnaireId" />
            <div class="form-group">
                <label>Вопрос</label>
                <textarea class="form-control" rows="3" asp-for="@Model.Question.Name">
                </textarea>
            </div>
            @if (Model.Question.QuestionCategoryId == 0)
            {
                <div class="form-group">
                    <label>Категория вопроса</label>
                    <select class="custom-select" asp-for="@Model.Question.QuestionCategoryId" asp-items="@Model.QuestionCategories">
                    </select>
                </div>
                <a class="btn btn-dark float-left"
                   asp-page="ConcreteQuestionnaire"
                   asp-route-questionnaireId="@Model.QuestionnaireId">
                    Текущая анкета
                </a>
            }
            else
            {
                <input type="hidden" asp-for="@Model.Question.QuestionCategoryId" />
                <a class="btn btn-dark"
                   asp-page="ConcreteQuestionnaire"
                   asp-route-questionnaireId="@Model.QuestionnaireId">
                    Анкета
                </a>
            }
            <button class="btn btn-success float-right">Отправить</button>
        </form>
    </div>
</div>