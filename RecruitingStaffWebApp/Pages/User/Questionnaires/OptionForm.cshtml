@page
@model RecruitingStaffWebApp.Pages.User.Questionnaires.OptionFormModel
@{
    ViewData["Title"] = Model.Option.Name;
}
<div class="row justify-content-center">
    <div class="col-5">
        <form method="post">
            <input type="hidden" name="questionId" asp-for="@Model.QuestionId" />
            <div class="form-group">
                <label class="col-form-label">Имя свойства</label>
                @if (Model.Option.Name == "")
                {
                    <input class="form-control" asp-for="@Model.Option.Name" />
                }
                else
                {
                    <input class="form-control" asp-for="@Model.Option.Name" readonly />
                }
                <span asp-validation-for="@Model.Option.Name"></span>
            </div>
            <div class="form-group">
                <label class="col-form-label">Значение свойства</label>
                <textarea class="form-control" rows="5" asp-for="@Model.Option.Value">
                    @Model.Option.Value
                </textarea>
                <span asp-validation-for="@Model.Option.Value"></span>
            </div>
            <a class="btn btn-dark"
               asp-page="/User/Candidates/Candidates">
                Список
            </a>
            <button class="btn btn-outline-dark float-right">Отправить</button>
        </form>
    </div>
</div>